---
parent:
---

# 1 Lead Gen

A top-level function. Its `parent` is written and EMPTY — the real vault's shape, and the
reason `function_top` has to fall back to the function itself rather than trust the key's
presence.
