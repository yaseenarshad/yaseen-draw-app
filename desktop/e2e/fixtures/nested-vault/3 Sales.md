---
parent:
---

# 3 Sales

A top-level function with no sub-functions. Problems naming it have an inner value EQUAL to
their outer one, so the merge rule keeps them directly under the outer section.
